# एडमिन-नियंत्रित डेटाबेस माइग्रेशन योजना (बैकअप सुविधा के साथ)

## अवलोकन

यह दस्तावेज़ डेटाबेस स्कीमा को प्रबंधित करने के लिए एक नई, सुरक्षित और नियंत्रित प्रणाली की योजना का विवरण देता है। इस प्रणाली में, माइग्रेशन प्रक्रिया से ठीक पहले एक **अनिवार्य स्वचालित बैकअप** चरण शामिल है। जब भी कोई प्रशासक (Admin) माइग्रेशन शुरू करेगा, तो सिस्टम पहले पूरे डेटाबेस का बैकअप बनाएगा और उसे सुरक्षित रूप से संग्रहीत करेगा, और उसके बाद ही स्कीमा में कोई परिवर्तन करेगा।

यह प्रणाली डेटा हानि के जोखिम को लगभग शून्य कर देती है और स्कीमा परिवर्तनों पर पूर्ण नियंत्रण और पारदर्शिता प्रदान करती है।

---

## मुख्य घटक (Core Components)

### 1. `schema.sql` - सत्य का एकमात्र स्रोत (Single Source of Truth)

- **स्थान:** प्रोजेक्ट की रूट डायरेक्टरी।
- **उद्देश्य:** यह फ़ाइल हमारे संपूर्ण डेटाबेस स्कीमा (`CREATE TABLE ...` स्टेटमेंट्स) को परिभाषित करेगी।

### 2. सुरक्षित बैकएंड API एंडपॉइंट्स

निम्नलिखित नए, सुरक्षित API एंडपॉइंट्स बनाए जाएँगे जो केवल `admin` भूमिका वाले उपयोगकर्ताओं के लिए ही सुलभ होंगे।

#### a. `POST /api/admin/database/backup` (मैनुअल बैकअप)

- **उद्देश्य:** एडमिन को किसी भी समय मैन्युअल रूप से डेटाबेस का बैकअप बनाने की अनुमति देता है।
- **कार्यप्रणाली:**
    1. `wrangler d1 export` का उपयोग करके डेटाबेस का SQL डंप बनाता है।
    2. बैकअप फ़ाइल को एक टाइमस्टैम्प नाम के साथ Cloudflare R2 स्टोरेज में `backups/` फ़ोल्डर में अपलोड करता है।
    3. सफलता का संदेश लौटाता है।

#### b. `GET /api/admin/database/check` (ड्राई रन)

- **उद्देश्य:** वास्तविक परिवर्तन किए बिना `schema.sql` फ़ाइल की तुलना लाइव डेटाबेस से करता है।
- **प्रतिक्रिया:** उन SQL कमांड्स की एक सूची लौटाता है जिन्हें चलाने की आवश्यकता है ताकि एडमिन बदलावों की समीक्षा कर सके।

#### c. `POST /api/admin/database/migrate` (बैकअप और निष्पादन)

- **उद्देश्य:** यह माइग्रेशन प्रक्रिया का मुख्य एंडपॉइंट है जिसमें सुरक्षा अंतर्निहित है।
- **कार्यप्रणाली:**
    1.  **अनिवार्य बैकअप:** सबसे पहले, यह आंतरिक रूप से बैकअप प्रक्रिया (जैसा कि `/backup` एंडपॉइंट में है) को चलाता है।
    2.  **सत्यापन:** यदि बैकअप प्रक्रिया विफल हो जाती है, तो यह तुरंत रुक जाता है और एक त्रुटि लौटाता है। **कोई स्कीमा परिवर्तन नहीं किया जाएगा।**
    3.  **सुरक्षित माइग्रेशन:** केवल बैकअप के सफल होने पर ही, यह `check` एंडपॉइंट के समान तर्क चलाकर स्कीमा में **योगात्मक (additive)** परिवर्तन (`CREATE TABLE`, `ALTER TABLE ADD COLUMN`) लागू करता है।
    4.  **विस्तृत लॉग:** यह पूरी प्रक्रिया (बैकअप और माइग्रेशन) का विस्तृत लॉग लौटाता है।

### 3. एडमिन पैनल UI (`/admin/database`)

एडमिन डैशबोर्ड में एक "डेटाबेस प्रबंधन" पेज बनाया जाएगा।

- **विशेषताएँ:**
    1.  **"बदलावों की जाँच करें" बटन:** ड्राई रन के लिए `check` API को कॉल करता है।
    2.  **"बैकअप और माइग्रेट करें" बटन:** मुख्य माइग्रेशन प्रक्रिया (`migrate` API) को शुरू करता है।
    3.  **"केवल बैकअप बनाएँ" बटन:** मैन्युअल बैकअप के लिए `backup` API को कॉल करता है।
    4.  **बैकअप सूची:** R2 से हाल के बैकअप की सूची, जिन्हें डाउनलोड भी किया जा सकता है।
    5.  **लाइव लॉग डिस्प्ले:** एक टेक्स्ट क्षेत्र जो बैकअप और माइग्रेशन की प्रगति को रीयल-टाइम में दिखाता है।

---

## अद्यतन कार्यप्रवाह (Admin Workflow)

1.  **डेवलपर:** `schema.sql` फ़ाइल में आवश्यक परिवर्तन करता है और कोड को डिप्लॉय करता है।
2.  **एडमिन:** `/admin/database` पेज पर जाता है।
3.  **एडमिन:** (वैकल्पिक) "केवल बैकअप बनाएँ" पर क्लिक करके एक तत्काल बैकअप बना सकता है।
4.  **एडमिन:** "बदलावों की जाँच करें" बटन पर क्लिक करके प्रस्तावित परिवर्तनों की समीक्षा करता है।
5.  **एडमिन:** "बैकअप और माइग्रेट करें" बटन पर क्लिक करता है।
6.  **सिस्टम (बैकएंड):**
    a. सबसे पहले, पूरे डेटाबेस का बैकअप बनाकर R2 में सहेजता है।
    b. बैकअप सफल होने पर, स्कीमा माइग्रेशन कमांड चलाता है।
7.  **सिस्टम (फ्रंटएंड):** एडमिन को पूरी प्रक्रिया का लाइव लॉग दिखाता है, जिसमें बैकअप और माइग्रेशन दोनों की स्थिति शामिल होती है।
